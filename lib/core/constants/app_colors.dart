import 'package:flutter/material.dart';

/// A class that holds all the color values for the app.
class AppColors {
  /// The primary color for the app (Civic Blue).
  static const Color primary = Color(0xFF1565C0);

  /// The accent color for the app (Sky Blue).
  static const Color accent = Color(0xFF00B8D4);

  /// The background color for the app .
  static const Color background = Color(0xFFEDEDED);

  /// The color for card elements (Pure White for cards).
  static const Color card = Color(0xFFFFFFFF);

  /// The color for dividers.
  static const Color divider = Color(0xFFE0E3E7);

  /// The primary text color.
  static const Color textPrimary = Color(0xFF1C1C1C);

  /// The secondary text color.
  static const Color textSecondary = Color(0xFF5F6368);

  /// The color for success states.
  static const Color success = Color(0xFF4CAF50);

  /// The color for warning states.
  static const Color warning = Color(0xFFFFB300);

  /// The color for error states.
  static const Color error = Color(0xFFE53935);
}
